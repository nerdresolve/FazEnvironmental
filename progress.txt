FAZ — Estado do progresso (checkpoint)

CONCLUÍDO NESTA SESSÃO (anterior):
- Splash screen na primeira visita: logo com fade+escala, 3 anéis "ripple" verdes se expandindo, som de sonar (1x, sincronizado com o primeiro anel, com destravamento por clique caso o navegador bloqueie autoplay), fade-out de 600ms no final.
- Scroll-reveal (fade+subida) aplicado em todas as seções da home e nas páginas de serviço, via componente <Reveal> reutilizável (IntersectionObserver).
- Logo faz.svg comprimida via SVGO (97.8KB -> 73KB).
- Compressão de vídeo do hero e correção do loop "corte seco" (desktop 18s).
- .gitignore criado e higienizado: node_modules, dist, logs de consentimento reais (mantida só a pasta via .gitkeep), .env, vídeos brutos de origem.
- Push realizado 2x para o GitHub (mariathdev/faz, repo privado) usando token de acesso pessoal (guardado em .claude/.env, nunca commitado).
- Imagem real (Wikimedia Commons, licença livre) aplicada na seção "Método FAZ" da home (helicóptero + navio de apoio offshore), como fundo com overlay escuro.

CONCLUÍDO NESTA SESSÃO (atual):
- Campo opcional `image` adicionado à interface ServiceDetail (apps/frontend/src/data/serviceDetails.ts).
- Imagens de header aplicadas nas páginas de serviço:
  - service-cpol-boom.jpg -> hero de /servicos/combate-poluicao-oleo
  - service-ics-firefighting.webp -> hero de /servicos/ics-100-200-300
  - ServiceDetailLayout.tsx agora renderiza fundo de imagem (padrão .method-bg, com overlay/gradiente) quando service.image existe; hero sem imagem continua com gradiente sólido como antes.
  - CSS: .service-detail-hero ganhou position:relative/overflow:hidden e .service-detail-hero .container ganhou z-index:1 para empilhar corretamente sobre o .method-bg.
- Hero-strip da home reposicionado: antes ficava espremido dentro do .container do hero, como rodapé do bloco de texto. Agora é uma faixa full-width própria (div.hero-strip) logo abaixo da section do hero, com fundo --color-navy-900 e bordas sutis, contendo .hero-strip-inner (mesmo conteúdo/CTA de antes). Arquivos: Home.tsx, global.css.
- Ícones temáticos adicionados aos 9 cards de serviço:
  - Novo componente apps/frontend/src/components/ServiceIcon.tsx com SVGs inline outline (sem nova dependência de lib de ícones), um por serviço (training, command, offshore, drill, coral, oil-spill, audit, maintenance, plan).
  - services.ts: campo `icon: ServiceIconKey` (obrigatório) adicionado a cada um dos 9 serviços.
  - ServiceCard.tsx: novo bloco .service-card-top com badge circular (.service-icon-badge, fundo verde translúcido, hover mais forte) ao lado do número.
- Build validado (tsc -b + vite build client/SSR + prerender) sem erros; HTML prerenderizado conferido via grep — confirma presença de hero-strip/hero-strip-inner, service-card-top, service-icon-badge e os 9 <svg class="service-icon">.
- Observação de ambiente: repo local não tinha node_modules instalado; rodado `pnpm install` (via `pnpm --filter faz-frontend build`) na raiz do monorepo pnpm para restaurar dependências antes do build funcionar.

OTIMIZAÇÃO DE IMAGENS (checagem pós-implementação):
- service-cpol-boom: estava só em .jpg (191 KB), sem .webp — corrigido. Gerado .webp via sharp (resize para largura 1100px + quality 70): 191 KB -> 150 KB. Import em serviceDetails.ts trocado de .jpg para .webp.
- service-ics-firefighting.webp: já existia mas estava em resolução desnecessária (1600x2404, retrato cheio) para uso como fundo de hero landscape a 22% opacidade. Reprocessado (resize para largura 1300px + quality 75): 78 KB -> 44 KB.
- Ferramenta usada: sharp instalado via npm --prefix num diretório de scratchpad isolado (não é dependência do projeto, não foi adicionado a package.json). Nenhum cwebp/ffmpeg disponível no ambiente.
- .jpg de origem mantidos em disco (não referenciados no código, apenas como fonte caso precise reprocessar).
- hero-video-desktop-KgcJ3dzt.mp4 (2.56 MB) segue sendo de longe o maior asset do build — já foi comprimido em sessão anterior, mas ffmpeg não está disponível neste ambiente para tentar reduzir mais. Se quiser cortar mais, precisa de ffmpeg instalado (não presente no PATH atual) ou reprocessar externamente.

CONCLUÍDO (rodada de imagens faltando — pós-deploy):
- Seção "Sobre" da home (SobreSection.tsx): o quadro .about-visual nunca teve imagem real, só gradiente decorativo + dot-grid. Adicionada imagem de fundo (about-response.webp, navio da Guarda Costeira dos EUA implantando barreira no canal de Galveston, domínio público) com overlay escuro + grid por cima (CSS: img z-index:0, ::after overlay z-index:1, ::before grid z-index:2).
- As 3 páginas de serviço que ainda não tinham imagem no hero (imo-i, oilrec-safety-check, resposta-offshore-pratica) agora têm, completando as 5 páginas com detail page:
  - service-imo-i.webp: skimmer sendo posicionado, Coast Guard Cutter Oak, Deepwater Horizon response.
  - service-oilrec-audit.webp: técnico da Marinha checando painel de controle de propulsão (USS Benfold) — usado para transmitir "auditoria/inspeção técnica".
  - service-offshore-practice.webp: equipe manuseando barreira de óleo para descontaminação (EPA, Deepwater Horizon response).
- Todas as imagens: Wikimedia Commons, domínio público (US Coast Guard / US Navy / US EPA, obras do governo federal americano), baixadas, redimensionadas para 1300px de largura e convertidas para .webp (quality 72-75) via sharp — mesmo padrão da rodada anterior.
- Build client+SSR+prerender validado; confirmado via curl que as 4 páginas (home + 3 serviços) servem HTTP 200 e o HTML prerenderizado referencia os novos arquivos de imagem corretamente.
- Nota de troubleshooting: o primeiro download da imagem para oilrec-audit veio consistentemente corrompido do Wikimedia (sharp: "premature end of JPEG image", mesmo com download 100% completo segundo curl) — trocada por outra imagem do mesmo tema em vez de insistir.

PENDENTE:
- Ainda não commitado/pushado para o GitHub nesta sessão — confirmar com o usuário antes de commitar (só commitar quando pedido explicitamente).
- Vídeo do hero (2.56 MB) é o maior gargalo de peso do site hoje; requer ffmpeg (ausente no ambiente atual) para nova rodada de compressão.
- Backend do FAZ agora roda em produção local via watchdog (scripts/watchdog.ps1 + Tarefa Agendada "FAZ-Watchdog", auto-start no logon), exposto publicamente em fazenvironmental.com.br via Cloudflare Tunnel (tunnel ed9f9363..., compartilhado com nerdresolve.com/.com.br). DNS já propagado e roteado corretamente.

ARQUIVOS-CHAVE TOCADOS NESTA SESSÃO (atual):
- apps/frontend/src/data/serviceDetails.ts
- apps/frontend/src/components/ServiceDetailLayout.tsx
- apps/frontend/src/pages/Home.tsx
- apps/frontend/src/data/services.ts
- apps/frontend/src/components/ServiceCard.tsx
- apps/frontend/src/components/ServiceIcon.tsx (novo)
- apps/frontend/src/styles/global.css

PRÓXIMO PASSO SUGERIDO:
Revisar visualmente no navegador (dev server: cd apps/frontend && npm run dev) as 3 mudanças de UX. Se aprovado, perguntar ao usuário se deseja commitar e dar push.
